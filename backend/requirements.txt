# 后端依赖。开发使用 conda `myself` 环境（Python 3.12），以下依赖均已在该环境装好。
# 此清单用于记录与在其他环境复现，不要在 myself 环境重复 pip install。
# 关键依赖锁大版本下限，防 major breaking（如 openai 1.x 的接口变更）。

fastapi>=0.115
uvicorn[standard]
pydantic>=2
pydantic-settings
openai>=1.85
neo4j>=6
sse-starlette
httpx
PyMuPDF
pytest
