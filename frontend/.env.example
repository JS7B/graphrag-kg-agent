# 后端 API 地址（开发期默认本地 FastAPI）
VITE_API_BASE_URL=http://localhost:8000

# 前端鉴权 key（配合后端 B2 的 X-API-Key 中间件）
# 留空=开发模式（后端 key 为空时自动放行）；部署时填真实值启用鉴权。
# 注意：SSE（EventSource）无法带 header，生产启用 key 后需后端为 /events/stream 放行或改用 fetch-based SSE。
VITE_API_KEY=
