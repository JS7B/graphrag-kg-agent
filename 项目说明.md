# 项目说明（交接 / 换机器恢复用）

> 本文件面向「在新机器上继续本项目」或「他人接手」的场景，回答四件事：
> **这是什么 · 做到哪了 · worktree 工作流怎么用 · 换机器怎么恢复 · 还有什么问题**。
> 日常启动步骤见 [`运行说明.md`](运行说明.md)；任务清单与历史见 [`tasks/todo.md`](tasks/todo.md)。
> 最后更新：2026-06-30。

---

## 一、项目是什么

**graphrag-kg-agent**（展示名 **Archigraph 档图** = archive + graph）—— 面向个人文档集合（技术论文 / GitHub 文档 / 产品需求文档）的端到端 **Agentic GraphRAG** 系统：
文档解析 → 切块 → 向量化 → 实体关系抽取 → Neo4j 知识图谱 → **ReAct 检索-反思 Agent**（LLM 自主决定检索什么/证据够不够/换查询再查，多工具自选）→ **带可追溯引用**的回答，配一个清晰专业、带「像素 Agent 房间」动效的前端工作台。定位：个人学习 + 简历展示 + 公开可复现。

**技术栈**：Python 3.11+（实际用 conda `myself` 环境，3.12）/ FastAPI / Pydantic · Neo4j 5.26 + Vector Index（Docker）· OpenAI-compatible LLM（不绑厂商）· 自研 ReAct 循环 + 原生 function calling（不引 LangGraph）· PyMuPDF · React 19 + Vite + TypeScript + Cytoscape.js。

**仓库**：https://github.com/JS7B/graphrag-kg-agent （公开）

---

## 二、整体进度（截至 2026-06-30）

| 板块 | 状态 | 说明 |
|---|---|---|
| 规划与范围确认 | ✅ | 决策全定（PDF=PyMuPDF、可视化=Cytoscape、LLM 占位不绑厂商） |
| 项目基础设施 | ✅ | git/Docker/Neo4j/.env.example/README 就绪 |
| 后端骨架与配置 | ✅ | FastAPI 分层、配置、日志、错误、健康检查、Neo4j/LLM 客户端 |
| 文档解析与切块 | ✅ | md/txt/PDF + chunker + 目录导入，偏移可追溯 |
| Neo4j 图谱与向量索引 | ✅ | 约束/向量索引、Document/Chunk 写入、embedding、向量召回 |
| 实体识别与关系抽取 | ✅ | LLM JSON 抽取、合并去重、MENTIONS/RELATES、证据可追溯 |
| **Agentic RAG 检索与回答** | ✅ | **ReAct 循环 + function calling**：vector_search/expand_entity 多工具自选、证据池、引用闭环、降级线性兜底 |
| 文档上传 API + Run/事件流 | ✅ | POST /api/documents、异步 Run（已 asyncio.to_thread 异步化）+ SSE 实时进度 |
| 前端工作台 | ✅ | 三视图（问答/文档库/图谱）+ 引用面板，**全链路接真实 API** |
| 像素 Agent 动画 | ✅ | ZCodeRoom 式房间：深紫房间 + 悬浮小人 + 场景道具叙事，rAF 中断即转，真实 RunEvent 驱动 |
| 评估与质量保障 | ✅ | 4 篇样本 + ground_truth + run_eval.py，量化四指标（实体召回 57.1% 待优化） |
| **PR 审计整改** | ✅ | 安全（API Key 鉴权/CORS/错误不泄露）、可复现（依赖补全/启动统一）、正确性（异步化/超时/维度校验）、前端无障碍 |
| **多轮对话记忆（对话进图谱）** | ✅ | **新**：Conversation/Message 节点存图谱 + 问答向量化 + 近期历史注入 LLM（滚动窗口）+ 多会话侧边栏，刷新可恢复历史 |
| **公开展示与简历材料** | ⬜ **未做** | 架构图、演示脚本、GIF、简历 bullet |

**一句话**：核心功能 + Agentic RAG 升级 + 多轮对话记忆 + 一轮 PR 审计加固全部完成且端到端验证过；**只剩「公开展示与简历材料」**（README/项目说明已完善，差架构图/演示/简历 bullet）。

---

## 三、worktree 多窗口工作流（本项目的开发方式）

本项目用 **「大脑 + 工人」多窗口并行** 开发，靠 git worktree 隔离：

```
E:\Mine\
├── graphrag-kg-agent\            [main]          ← 大脑窗口：规划/评审/合并/维护 main
├── graphrag-kg-agent-backend\    [feat/backend]  ← 后端工人窗口
└── graphrag-kg-agent-frontend\   [feat/frontend] ← 前端工人窗口
```

**硬约定**（详见 CLAUDE.md / AGENTS.md「多窗口协作约定」）：
1. 每条工作线 = 一个独立 worktree + 一个固定分支 `feat/<板块>`，人工建、全程不变。
2. 工人窗口只在自己分支 commit，**绝不碰 main、不自行合并**。
3. 交接信号 = 工人本地 commit + 口头通知大脑分支名 → 大脑读 diff 评审。
4. **合并权统一在大脑窗口**，评审通过才 merge 回 main。
5. 工人开工前先 `git merge main` 同步（否则基线落后，合并时出「误删假象」）。
6. 共享资源错峰：Neo4j（同一容器同库）、端口（后端 8000 / 前端 5173）跨 worktree 共享，并行写图谱/起服务要错开；`.env` 不随 worktree 复制，需手动放。
7. 收工回收 worktree + 工人启动的后台进程（uvicorn/vite 残留会占目录与端口）。

**学习记录约定（DEVLOG）**：引入新工具/配置/框架或完成非显然工作流后，追加学习记录——板块的写进该板块目录 `DEVLOG.md`（`backend/DEVLOG.md`、`frontend/DEVLOG.md`），跨板块/基础设施写进 `docs/DEVLOG.md`。

---

## 四、换机器恢复步骤（关键）

> **好消息：所有工作已全部合入 `main` 分支**（两条 feat 分支领先 main 均为 0），所以新机器只 clone main 就拿到全部代码，**不丢任何东西**。worktree 是本地机制，远程没有 feat 分支，新机器重新建即可。

### 1. clone + 装环境

```bash
git clone https://github.com/JS7B/graphrag-kg-agent.git
cd graphrag-kg-agent
```

环境前提（新机器要装）：
- **Docker Desktop**（Windows 需 WSL2 后端）
- **Anaconda/conda** + 一个 Python 3.11+ 环境（本项目原用名为 `myself` 的环境）
- **Node.js 18+**

### 2. 配 .env（必须，否则 LLM 功能跑不通）

```bash
cp .env.example .env
```
**务必填真实值**（当前最大坑，见第五节）：`OPENAI_BASE_URL` / `OPENAI_API_KEY` / `CHAT_MODEL` / `EMBEDDING_MODEL` / `EMBEDDING_DIM`（与 embedding 模型维度一致）/ `NEO4J_PASSWORD`。

### 3. 装依赖

```bash
# 后端（在你的 conda 环境，如 myself）
cd backend && pip install -r requirements.txt && cd ..
# 前端
cd frontend && npm install && cd ..
```

### 4. 启动（详见 运行说明.md）

```bash
docker compose up -d neo4j                                  # 1. Neo4j
cd backend && python -m uvicorn app.main:app --reload --port 8000   # 2. 后端
cd frontend && npm run dev                                  # 3. 前端 → localhost:5173
```

### 5.（可选）重建 worktree 继续多窗口开发

```bash
git worktree add ../graphrag-kg-agent-backend  -b feat/backend  origin/main
git worktree add ../graphrag-kg-agent-frontend -b feat/frontend origin/main
# 注意：每个新 worktree 要手动 cp .env 进去（前端还要各自 npm install）
```

---

## 五、已知问题与遗留事项

### ⚠️ 必须先处理（否则功能跑不通）
- **`.env` 的 LLM 配置**：换机器后 `CHAT_MODEL`/`EMBEDDING_MODEL` 等若仍是占位值 `your-xxx`，`/health/deps` 会显示 `llm: not_configured`，**问答、实体抽取、文档完整入库全部失败**（上传会卡在 embedding 报 Connection error）。CHAT_MODEL 需支持 JSON 输出模式（`response_format json_object`）。

### 📋 待办板块
- **公开展示与简历材料**（唯一未动板块）：完善 README、绘架构图、演示脚本、录 GIF、整理简历 bullet。

### 🔧 待优化（非阻塞，质量项）
- **实体召回率 57.1% < 70% 目标**（评估实测）：LLM 抽取漏了部分技术名词（React/Vite/TypeScript 等）。可调抽取 prompt 或加近义合并。其余指标达标（关系可用 100%、引用命中 100%、幻觉 16.7%）。这是模型/prompt 调优问题，结合 `evals/` 单独迭代。
- **多轮对话记忆的真实多轮手测**（2026-06-30 新增）：多轮记忆的代码逻辑（近期历史注入、会话隔离、持久化恢复）已逐条回源码核实 + 单测覆盖，但真实多轮 LLM 调用因 feat/backend worktree 配额耗尽（403）没能实跑。**换 key / 配额恢复后建议做一次真实端到端手测**：上传文档 → 同一会话连续追问 → 看第二轮能否正确指代上文、刷新后历史是否恢复。
- **turn_index 并发隐患**（2026-06-30 新增）：`_NEXT_TURN`（读 max+1）与 `_ADD_MESSAGE`（MERGE）非原子，同一会话并发写理论上可能算出相同 turn。当前受 `_LLM_SEMAPHORE(3)` + 单用户串行写保护实际不撞，且幂等 MERGE by message_id 保证不翻倍。记录在案。
- **会话标题**取首问前 30 字，未来可让 LLM 生成更智能的标题。
- **SSE 与 API Key 鉴权冲突（部署前待办，见 lessons L7）**：浏览器 `EventSource` 不支持自定义 header，一旦部署配了 `API_KEY`，SSE 进度流会被 401 挡住。开发模式（API_KEY 为空）无影响。公网部署需另解（token 走 query / cookie 鉴权）。
- **rerank**：RERANK_MODEL 不可用时问答自动降级为按向量分排序，不影响主链路。
- **测试向量索引隔离（B10）**：原计划独立索引名物理隔离，被 Neo4j「单 property 单向量索引」限制否决，现用 teardown 恢复维度 + lifespan 启动维度校验双兜底（见 lessons L6 + backend/DEVLOG）。

### ✅ 已由 PR 审计整改修复（2026-06-28）
- requirements 补 sse-starlette + 锁版本（解除第三方装完起不来）、启动命令三份文档统一去绝对路径。
- 后端加 API Key 鉴权 + CORS 限定来源 + 500 不泄露内部、LLM 超时重试空响应防护。
- BackgroundTasks 用 asyncio.to_thread 异步化（恢复 SSE 实时性）+ 跨线程安全 emit、RunStore TTL GC、embedding 维度运行时校验。
- RunEvent `timestamp_ms` 已加 alias 统一为 `timestampMs`、前端无障碍 7 项 CRITICAL + 响应式断点。

### ✅ 已验证可跑通
- 完整启动链路（Neo4j→后端→前端）+ 真实 Agentic RAG 多轮问答端到端实测通过。
- 后端全量测试 **140 passed + 6 skip**（真实 LLM 测试因 main 无 key 而 skip；feat/backend worktree 配 key 时真实 LLM 用例会跑，受配额影响可能 403）。

---

## 六、关键文件导航

| 文件 | 作用 |
|---|---|
| `运行说明.md` | 日常启动步骤 + 常见问题 |
| `tasks/todo.md` | 可勾选任务清单 + 完整 Review 历史（每个板块怎么做的） |
| `tasks/lessons.md` | 纠错教训 |
| `docs/personal-kg-graphrag-agent-plan.md` | 总规划：定位/概念模型/图谱设计/流程/API/评估 |
| `docs/evaluation.md` | 评估集构成、指标定义、复现方式 |
| `docs/DEVLOG.md` · `backend/DEVLOG.md` · `frontend/DEVLOG.md` | 分层学习记录（工具/概念/为什么这么做/踩坑） |
| `CLAUDE.md` / `AGENTS.md` | 给 AI 代理的硬规则、决策边界、协作约定（两份同步，分别面向 Claude Code / 通用 Agent） |
| `frontend/docs/agent-room-*.md` | 像素 Agent 房间式改造的规格与交接 |
